import SvgIcon, { SvgIconProps } from "@mui/material/SvgIcon";
import { freetag, back } from "./assets";

export default function PaginatedItems(props: SvgIconProps) {
  return <img src={back} alt="aa" style={{ width: "40%" }} />;
}
